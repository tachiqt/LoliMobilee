<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="8dp"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:strokeColor="@color/light_gray"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Date and Time of the Entry -->
        <TextView
            android:id="@+id/journalEntryDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="March 20, 2024"
            android:textSize="14sp"
            android:textColor="@color/purple_500"
            android:textStyle="bold" />

        <!-- Entry Title -->
        <TextView
            android:id="@+id/journalEntryTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Reflecting on My Day"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/dark_purple" />

        <!-- Entry Preview -->
        <TextView
            android:id="@+id/journalEntryPreview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:maxLines="2"
            android:ellipsize="end"
            android:text="This is a preview of the journal entry content, providing a quick glimpse of what was written."
            android:textSize="14sp"
            android:textColor="@color/violet" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end"
            android:layout_marginTop="8dp">

            <ImageView
                android:id="@+id/editIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginEnd="8dp"
                android:src="@drawable/edit"
                android:contentDescription="Edit Entry"
                app:tint="@color/purple_700" />

            <ImageView
                android:id="@+id/deleteIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/delete"
                android:contentDescription="Delete Entry"
                app:tint="@color/purple_700" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
